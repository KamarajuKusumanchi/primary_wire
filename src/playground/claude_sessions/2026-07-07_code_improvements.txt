2026-07-07 11:29 AM

Prompt:
Read src/scrape_q4_ir.py src/scrape_investorroom.py src/scrape_notified.py, sources/sources.yaml in https://github.com/KamarajuKusumanchi/primary_wire/archive/refs/heads/master.zip and any files they touch. Is there anything you would do to make the code more modular, readable, simple and more future proof? I want to do this before adding scrapers for other IR platforms.

Response:
This one's already in decent shape. Let me look at the other two scrapers and the shared utils.Now let's look at scrape_notified.py, the largest one.Now let's look at the utils directory and sources.yaml.Now let's check csv_utils and sources_utils, plus sources.yaml.Now let's look at sources.yaml and detect_ir_platform.py (mentioned to touch these), plus scrape_all.py which is the orchestrator.I read through all three scrapers, the shared `utils/` modules, `sources.yaml`, `scrape_all.py`/`scraper_config.yaml`, the tests, and the legacy per-company wrapper scripts. Overall verdict: the shared-utils layer (`scrape_utils.py`, `csv_utils.py`, `sources_utils.py`) is genuinely well factored — good docstrings, sensible precedence rules, no obvious duplication *within* it. But there are real structural problems that will bite you specifically when you add a 4th platform. In priority order:

**1. Delete `scrape_cdw.py`, `scrape_costco.py`, and `scrape_company_template.py`.**
These are dead weight that actively works against you. `scrape_all.py` + `scraper_config.yaml` already generically dispatch any slug into the right scraper module by reading `sources.yaml` (`ir_url`, `ticker`, `needs_detail_page_dates`, etc.) — that's the real, current mechanism for adding a source. The three wrapper files do the exact same resolution by hand, are never invoked by `scrape_all.py`, and `scrape_company_template.py`'s docstring literally instructs future contributors to "copy this file per company" — the precise anti-pattern the config-driven system was built to replace. If someone follows that template for platform #4, you're back to N files per N companies. This is the single highest-leverage cleanup before expanding: it makes "how do I add a source" unambiguous (one `sources.yaml` record + one `scraper_config.yaml` entry, full stop).

**2. The three real scrapers duplicate their own control flow, not just constants.**
`scrape_utils`/`csv_utils`/`sources_utils` correctly share *helpers*. But the outer shape — build CLI → resolve source → loop over years/pages → dedupe by URL → optionally fetch detail pages → filter → preview → write CSV/JSON → `main()` — is reimplemented three times with only the parsing internals differing. Compare `scrape_investorroom.scrape()`/`scrape_notified.scrape()`: both do "global dedup across year passes" with the identical 5-line seen-set block, both scraper's `main()` bodies are ~40 lines that are 80% identical to each other. Before writing a 4th platform module, I'd pull this into something like a small `run_scraper(platform_config)` in `scrape_utils.py` that takes: a `parse_listing_page(html) -> (items, next_url_or_None)` callable, a `fetch_html` callable, and a `fetch_date_from_detail_page` callable — and owns the CLI, the pagination loop, dedup, filtering, and output. Then a new platform module becomes ~100 lines of platform-specific parsing instead of ~500 lines that re-derive the same skeleton. Right now you're one copy-paste away from a bug fix (e.g. the dedup logic, or the `--format json` output-path check) landing in two of three files and silently not in the third.

**3. `scrape_q4_ir.py` doesn't use the HTTP-scraper shared helpers, and nobody would notice if a new module made the same mistake.**
`add_network_and_debug_args`, `configure_logging`, and `fetch_missing_dates_via_http` exist in `scrape_utils.py` but are only used by `scrape_investorroom.py`/`scrape_notified.py`. `scrape_q4_ir.py` has its own hand-rolled logging setup (`-v`/`-vv` counted verbosity vs. the others' boolean `-v`/`--verbose`) and its own `_launch_browser`/`fetch_missing_dates`. That's defensible given Playwright is a genuinely different transport — but the result is that today, across three scrapers, there are two incompatible `-v` conventions and two different "fetch missing dates" call shapes, and nothing enforces consistency. A future platform author copying whichever scraper they open first will silently propagate whichever inconsistency they picked. Worth either unifying the CLI surface (even if the fetch mechanics stay separate) or explicitly documenting in one place which convention is canonical.

**4. Regex/URL-shape duplication between each scraper and `detect_ir_platform.py`.**
`detect_ir_platform.py` copies `NEWS_LINK_RE`, `DETAIL_URL_LEGACY_RE`/`DETAIL_URL_MODERN_RE`, and `DETAIL_URL_RE` verbatim from the three scrapers, with a comment saying "taken verbatim from each scraper's source" to keep them in sync by hand. That's a maintenance trap: today it's 3 regexes duplicated in 2 places each; a 4th platform makes it 4 regexes × 2 places, kept in sync by comment discipline alone. These should live once (e.g. as `Q4_NEWS_LINK_RE` etc. exported from each scraper module, or better, hoisted into a small shared "platform fingerprints" module) and be imported by `detect_ir_platform.py` rather than copied.

**5. Test coverage is dangerously thin for exactly the logic you're about to replicate.**
The only tests that exist cover `join_url_path()` and `scrape_all.py`'s smoke-test grouping. There is zero test coverage for: `parse_date`/`parse_time`/`DATE_PATTERNS`, `extract_date_from_detail_html`, `filter_items`, `sort_key`/`merge_into_daily_csvs`, `resolve_source_identity`, or any of the three `parse_listing_page`/`parse_news_items` functions. These are precisely the pieces that (a) are hardest to get right (date-format edge cases, ancestor-walking heuristics, dedup-by-URL), and (b) every new platform scraper will copy-and-modify. Right now a change to `parse_date`'s regex order, or to `filter_items`' date-exclusion logic, has no safety net and could silently corrupt data months later. Before adding platform #4, I'd add parametrized tests against small saved HTML fixtures for each `parse_listing_page`, plus straightforward unit tests for the date/time regex module — that's the actual thing standing between you and "I added a scraper and it quietly mis-dated things."

**Smaller stuff, not blocking:**
- `scrape_notified.py`'s `scrape_one_pass` and `scrape_all.py`'s dedup-by-`rstrip('/')` pattern appears 3+ times verbatim across `scrape_notified.py`/`scrape_investorroom.py` (`scrape()` at the bottom of `scrape_notified.py` duplicates the same 5-line dedup block twice within the same function). Trivial to factor into a `_dedupe_by_url(items)` helper in `scrape_utils.py`.
- `NewsItem.to_csv_row()` in `scrape_q4_ir.py` is marked "kept for backward compatibility" but nothing in the repo calls it except `to_row()` itself — worth confirming it's actually dead and removing, or it'll get copied into platform #4 out of caution.

If I were sequencing this: **#1 (delete the wrapper scripts) and #5 (tests for parse/date logic) first** — they're cheap and directly de-risk what you're about to do. **#2 (shared scraper skeleton)** is the biggest win but also the biggest diff; worth doing before you write platform #4's `main()`, not after, since retrofitting it across four scrapers later is more painful than three.