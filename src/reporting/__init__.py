"""Read-only diagnostic scripts for primary_wire.

Scripts here fetch or read data but never write to data/, sources.yaml, or
config/scraper_config.yaml. See docs/reporting.txt (at the repo root) for
the full convention, including what to do when adding a new script here.
"""